# 包参谋 AI 技能中心设计系统

## 1. 视觉主题与氛围

产品是一件高频桌面工具，不是品牌展厅。选定方向为“静默清单”：温暖白底、轻量侧栏、列表优先、克制单色。用户打开后 3 秒内应看到技能数量、搜索入口、治理状态和技能列表；品牌只通过“方策”图标与一处战略黄行动点被记住。

## 2. 色板与角色

- 应用底色：`#f7f6f2`，温暖但接近白色。
- 内容面：`#ffffff`；次级面：`#f1f0eb`。
- 主文字：`#20201c`；正文：`#55544e`；次要文字：`#85827a`。
- 结构线：`#e2e0d8`；强边界：`#c7c4b9`。
- 战略黄：`#e5b91e`，仅用于方策图标、主操作、键盘焦点和选中提示。
- 成功、警告、失败使用独立语义色；品牌黄不冒充状态色。

## 3. 排版规则

- 中文 UI 使用系统字体栈：`PingFang SC → Hiragino Sans GB → Microsoft YaHei → Noto Sans SC`。
- 字重只用 400 / 500 / 600；标题不靠夸张字号制造层级。
- 技能采用“中文主名＋英文原 ID”双层结构，英文 ID 使用等宽字体。
- 中文正文不使用负字距和斜体；数字使用 tabular numerals。
- 一级标题 22–26 px，技能名 13–14 px，正文与按钮不低于 12 px。

## 4. 组件样式

- 侧栏：浅暖灰，与主画布只用 1 px 分隔；选中项使用整块白底、全边框与字重，不使用左侧竖线。
- 资产概览：单行标题＋少量统计＋主操作；禁止 Hero、口号、策略轴和指标卡阵列。
- 资产列表：默认列表视图，以 hairline 分隔；悬停只改变底色和边界，不浮起。
- 按钮：主操作用墨黑底，黄色仅作小面积行动点；功能按钮 5–6 px 圆角，无重阴影。
- 输入：白底、1 px 边框、6 px 圆角；聚焦使用完整焦点环。
- 弹层：8–10 px 圆角和低透明多层阴影，只在确有 z 轴语义时使用。

## 5. 布局原则

- 默认窗口 `1180 × 760`，最小窗口 `900 × 640`。
- 侧栏 184 px，折叠后 60 px；主内容 `min-width: 0`。
- 首屏固定顺序：资产概览 → 搜索与筛选 → 技能列表。
- 使用 8 px 主节奏，取值集中于 4 / 8 / 12 / 16 / 24 / 32 px。
- 搜索是筛选区第一元素；低频筛选与视图切换靠右，避免同权按钮墙。

## 6. 深度层级

- 页面与侧栏均为平面层；普通列表不使用阴影。
- 容器边界采用 `1 px #e2e0d8`，或 Vercel 式 `0 0 0 1px rgba(0,0,0,.08)` ring。
- 只有菜单、弹窗和 toast 使用阴影；阴影透明度不高于 10%。
- 禁止纸纹、网格、光晕、渐变背景和大面积深色品牌块。

## 7. Do / Don't

- Do：让技能资产和搜索成为第一视觉焦点。
- Do：中文主名，英文兼容；保留英文目录与 `SKILL.md`。
- Do：一个区域只有一个主操作；图标必须在 16 px 仍可辨认。
- Don't：大黑块、品牌口号、仪表盘式指标阵列、装饰路径、伪数据和系统自言自语。
- Don't：三等分卡片、左侧竖线装饰、重阴影、渐变、霓虹、玻璃拟态和过量圆角。

## 8. 响应式

- `1080 px` 以下侧栏缩为 168 px；`940 px` 以下隐藏次要描述、标签和更新时间。
- 最小窗口仍保留中文名、英文 ID、使用范围、工具编组摘要和必要操作；逐工具图标进入卡片视图或详情页查看。
- 卡片视图可用，但首次进入固定为列表视图。
- 弹窗、文件树和 Markdown 阅读区各自滚动，页面不得横向溢出。

## 9. Motion 哲学

- 高频操作几乎静止，仅保留 100–160 ms 的颜色、背景和按压反馈。
- 使用 `cubic-bezier(0.23, 1, 0.32, 1)`，禁止 `transition: all` 和 `ease-in`。
- 不做列表入场动画，不做滚动叙事；必须支持 `prefers-reduced-motion`。

## 参考 DNA

- Vercel：吸收 `#171717` 非纯黑文字、6 px 功能圆角、400/500/600 三档字重、1 px ring 边界和极少颜色原则。
- Linear：吸收 8 px 节奏、单一重音色、低亮度差表达状态和工具级信息密度；不复制其深色首页、紫色与 Inter 字体。
