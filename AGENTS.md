# 包参谋 AI 技能中心开发规则

## 产品边界

- 本项目只维护包参谋 v1.0 自研核心，不重新引入旧项目模块。
- 中心源默认为 `~/.agents/skills`，任何操作不得删除或覆盖中心技能。
- 外部能力情报只做索引和人工复核，不打包、不自动安装第三方 Skill。
- 第三方 Skill 的来源与许可必须保真，不得表述为包参谋原创。

## 架构

- `src/App.tsx`：界面与交互。
- `src/appLogic.ts`：可独立测试的前端筛选与双语显示逻辑。
- `src-tauri/src/center.rs`：盘点、中文理解、评分、预览、工具连接与安全边界。
- `featured-skills.json`：外部索引数据，不是 Skill 源码库。

## 强制规则

- 中文主名与英文 ID 同时保留。
- 每个 Skill 必须在卡片第一层直接展示用途和特点；真实截图只作详情补充，没有图片时不制造效果图。
- macOS/Linux 只创建软链接；Windows 复制退化必须写入管理标记。
- 断开连接只能移除指向中心源的链接或带本应用标记的副本。
- 任务结束前执行 `npm run check`；发布前构建 DMG 并真实启动验收。
